

<%= form_for(@notice) do |f| %>
  <% if @notice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notice.errors.count, "error") %> prohibited this notice from being saved:</h2>

      <ul>
      <% @notice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <h1>Create/Update Notice</h1>

    <%= f.text_field :titile, class: "login-input", placeholder: "Title", required: true  %>

    <%= f.text_field :description, class: "login-input", placeholder: "Description", required: true %>

    <%= f.hidden_field :user_id, value: current_user.id %>

    <div><%= f.submit "Create/Update Notice", class:"login-submit" %></div>

    <p class="login-help"><a href="/notices">Back</a></p>
<% end %>
